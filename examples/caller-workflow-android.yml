# File mau cho project Android native (khong dung Flutter).

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  mobile-ci:
    uses: gk182/ci-templates/.github/workflows/mobile-ci.yml@main
    with:
      project_type: android
      build_ios: false
      run_deploy: ${{ github.ref == 'refs/heads/main' }}
      deploy_target: play_internal
    secrets: inherit

